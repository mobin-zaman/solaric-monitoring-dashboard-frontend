/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["img1.igen-tech.com", "i.ibb.co", "img1.solarmanpv.com"],
  },
};

module.exports = nextConfig;
